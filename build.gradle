// Top-level build file where you can add configuration options common to all sub-projects/modules.

// Repositories are needed at the root for the Kotlin Multiplatform native
// commonizer (`:commonizeNativeDistribution`), which resolves its classpath
// against the root project.
allprojects {
    repositories {
        mavenCentral()
    }
}
