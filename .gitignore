# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Log/OS Files
*.log

# Android Studio generated files and folders
captures/
.externalNativeBuild/
.cxx/
*.apk
output.json

# IntelliJ
*.iml
.idea/
misc.xml
deploymentTargetDropDown.xml
render.experimental.xml

# Keystore files
*.jks
*.keystore

# Google Services (e.g. APIs or Firebase)
google-services.json

# Android Profiling
*.hprof

# Jazzer fuzzing artifacts (ZIP321FuzzTest, run locally with JAZZER_FUZZ=1 or via CI's
# fuzz-smoke job): the seed corpus directory and any crash/timeout reproducer files it drops in
# the working directory (module root when run via `./gradlew :lib:jvmTest`).
.cifuzz-corpus/
crash-*
timeout-*
oom-*
